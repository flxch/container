// Go package providing the generic type Result[T].  The new type allows one to store
// a value and an error.  One of its use is to wrap the return values of
// functions with return pair (T, error).

package result
