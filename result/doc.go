package result

// Go package with the generic type Result[T].  The new type allows one to store
// a value and an error.  One of its use is to wrap the return values of
// functions that return pairs (T, error).
